# Example proxy configuration
# Copy this file to proxy-configs directory and modify as needed

# Example 1: Proxy to external service (for testing)
server {
    listen 80;
    server_name httpbin.local localhost;
    
    location /httpbin/ {
        proxy_pass http://httpbin.org/;
        proxy_set_header Host httpbin.org;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto http;
    }
}

# Example 2: Proxy to local development server running on host
# server {
#     listen 80;
#     server_name myapp.local;
#     
#     location / {
#         proxy_pass http://host.docker.internal:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto http;
#     }
# }

# Example 3: Proxy with path rewriting
# server {
#     listen 80;
#     server_name api.local;
#     
#     location /api/ {
#         proxy_pass http://host.docker.internal:8080/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto http;
#     }
# }